{
  "name": "postgres-driver-ts",
  "version": "0.0.1",
  "description": "A TypeScript library for PostgreSQL database interaction",
  "main": "driver.ts",
  "scripts": {
    "build": "tsc",
    "start.md5": "node --env-file=.env.auth-md5 dist/src/index.js",
    "start.scram": "node --env-file=.env.auth-scram dist/src/index.js",
    "test": "tsc && node --test \"dist/test/*.test.js\""
  },
  "keywords": [
    "node",
    "library",
    "module"
  ],
  "author": "Kretts",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.2.0"
  },
  "engines": {
    "node": ">=14"
  },
  "type": "module",
  "dependencies": {
    "typescript": "^5.9.2"
  }
}